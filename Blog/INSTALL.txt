# ============================================
# Instrukcje instalacji
# ============================================

SZYBKI START - INSTALACJA NA SERWERZE
======================================

1. PRZYGOTOWANIE BAZY DANYCH
   ---------------------------
   mysql -u root -p
   
   CREATE DATABASE blog_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   CREATE USER 'blog_user'@'localhost' IDENTIFIED BY 'TWOJE_BEZPIECZNE_HASLO';
   GRANT ALL PRIVILEGES ON blog_db.* TO 'blog_user'@'localhost';
   FLUSH PRIVILEGES;
   EXIT;

2. IMPORT SCHEMATU
   ---------------
   mysql -u blog_user -p blog_db < database/schema.sql

3. KONFIGURACJA
   ------------
   Edytuj config.php:
   
   - DB_USER: 'blog_user'
   - DB_PASS: 'TWOJE_HASLO'
   - DB_NAME: 'blog_db'
   - SITE_URL: 'https://twoja-domena.pl'
   - ADMIN_EMAIL: 'twoj@email.pl'
   - SECRET_KEY: wygeneruj losowy ciąg (min 32 znaki)
   - DEBUG_MODE: false (WAŻNE na produkcji!)

4. UPRAWNIENIA (Linux)
   --------------------
   chmod 755 Blog/
   chmod 644 Blog/*.php Blog/*.html Blog/*.css
   chmod 755 Blog/api/ Blog/database/
   chmod 644 Blog/api/*.php
   chmod 600 Blog/config.php

5. BEZPIECZEŃSTWO
   --------------
   ⚠️ ZMIEŃ HASŁO ADMINA!
   
   php -r "echo password_hash('NOWE_HASLO', PASSWORD_DEFAULT);"
   
   Potem w MySQL:
   UPDATE users SET password_hash = 'WYGENEROWANY_HASH' WHERE username = 'admin';

6. TEST
   ----
   Otwórz: https://twoja-domena.pl/Blog/
   
   Przetestuj:
   - Formularz kontaktowy: /contact.html
   - Komentarze: /post.html


INSTALACJA LOKALNA (XAMPP/MAMP)
================================

1. Skopiuj folder Blog/ do:
   - Windows: C:\xampp\htdocs\
   - Mac: /Applications/XAMPP/htdocs/ lub /Applications/MAMP/htdocs/

2. Uruchom XAMPP/MAMP Control Panel
   Start: Apache + MySQL

3. Otwórz phpMyAdmin: http://localhost/phpmyadmin
   - Utwórz bazę: blog_db
   - Import → database/schema.sql

4. Edytuj config.php:
   define('DB_USER', 'root');
   define('DB_PASS', '');  // Puste na XAMPP
   define('SITE_URL', 'http://localhost/Blog');
   define('DEBUG_MODE', true);

5. Otwórz: http://localhost/Blog/


STRUKTURA PLIKÓW
================

Blog/
├── index.html              # Strona główna
├── post.html               # Pojedynczy post
├── about.html              # O mnie
├── contact.html            # Kontakt
├── styles.css              # Style CSS
├── config.php              # ⚙️ KONFIGURACJA (EDYTUJ!)
├── functions.php           # Funkcje PHP
├── .htaccess               # Konfiguracja Apache
├── README.md               # Dokumentacja
├── INSTALL.txt             # Ten plik
├── api/
│   ├── comments.php        # API komentarzy
│   ├── contact.php         # API formularza
│   └── newsletter.php      # API newslettera
├── js/
│   └── blog.js             # JavaScript
└── database/
    └── schema.sql          # Schemat bazy danych


DOMYŚLNE DANE
=============

Admin:
  Login: admin
  Email: admin@blog.pl
  Hasło: admin123
  
⚠️ ZMIEŃ TO NATYCHMIAST!


TROUBLESHOOTING
===============

Problem: "Błąd połączenia z bazą danych"
-----------------------------------------
✓ Sprawdź dane w config.php
✓ Sprawdź czy MySQL działa
✓ Sprawdź uprawnienia użytkownika

Problem: Formularze nie działają
---------------------------------
✓ Sprawdź ścieżkę do plików API
✓ Sprawdź uprawnienia folderów (755/644)
✓ Włącz DEBUG_MODE i sprawdź błędy

Problem: 404 na API
-------------------
✓ Sprawdź czy mod_rewrite działa
✓ Sprawdź .htaccess
✓ Sprawdź uprawnienia folderu /api/


WSPARCIE
========

Logi:
- PHP: /var/log/php/error.log
- Apache: /var/log/apache2/error.log
- MySQL: /var/log/mysql/error.log

Włącz DEBUG_MODE w config.php aby zobaczyć szczegóły błędów.


BEZPIECZEŃSTWO - CHECKLIST
===========================

Przed uruchomieniem na produkcji:

[ ] Zmień hasło admina w bazie danych
[ ] Zmień SECRET_KEY w config.php
[ ] Ustaw DEBUG_MODE = false
[ ] Zmień DB_USER i DB_PASS na bezpieczne
[ ] Włącz HTTPS (certyfikat SSL)
[ ] Zabezpiecz dostęp do config.php
[ ] Zabezpiecz folder /database/
[ ] Ustaw poprawne uprawnienia (chmod)
[ ] Skonfiguruj backupy bazy danych
[ ] Przetestuj wszystkie formularze


BACKUP BAZY DANYCH
===================

Regularnie wykonuj backupy:

mysqldump -u blog_user -p blog_db > backup_$(date +%Y%m%d).sql

Przywracanie:
mysql -u blog_user -p blog_db < backup_20260207.sql


KONTAKT
=======

W razie problemów sprawdź README.md lub logi serwera.

Powodzenia! 🚀
